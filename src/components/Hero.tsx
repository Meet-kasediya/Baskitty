const Hero = () => {
  return (
    <section className="bg-light py-5 text-center">
      <div className="container">
        <h1 className="display-4 fw-bold">Welcome to Min React App</h1>
        <p className="lead mb-4">
          This is a minimal React + Bootstrap app deployed on Railway.
        </p>
        <a href="#features" className="btn btn-primary btn-lg">
          Learn More
        </a>
      </div>
    </section>
  );
};

export default Hero;
